{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - Build and debug active file",
            "type": "lldb",
            "request": "launch",
            "program": "build/myapp",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build",
        }
    ]
}
